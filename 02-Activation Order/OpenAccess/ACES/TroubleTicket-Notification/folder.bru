meta {
  name: TroubleTicket-Notification
  seq: 10
}

docs {
  # ACES â€” Trouble Ticket Notification

  Notification flow when ACES handles a SR/Trouble-Ticket lifecycle:

  Create / ReOpen â†’ Resolved   (success path)
  Create / ReOpen â†’ Rejected   (failure path)

  The two `.bru` files mirror these two terminal outcomes.

  - **Resolved**  â€” `providerStatus = "Resolved"`,  `providerComments = "ODB Port Clean"`
  - **Rejected**  â€” `providerStatus = "Rejected"`,  `providerComments = "ODB Port Clean not successful"`
}

