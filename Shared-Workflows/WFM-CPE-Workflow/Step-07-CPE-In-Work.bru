meta {
  name: Step-07 CPE - In Work
  type: http
  seq: 7
}

post {
  url: {{demo-mob-dev}}/workForceManagement/v1/notification/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "eventId": "Event-{{orderId}}",
    "eventTime": "{{eventDate}}",
    "eventType": "workOrderStateChangeNotification",
    "event": {
      "workOrder": {
        "id": "{{workOrderIdCpe}}",
        "externalId": "{{orderId}}",
        "state": "IN WORK",
        "partition": "VIRTUALVENDOR_IW",
        "region": "CENTER-CENTER_1_IW",
        "vendorName": "VIRTUALVENDOR_IW",
        "engineerId": "EnggInWork",
        "engineerName": "MOTTEST03_IW",
        "engineerPhone": "+966-56-0019901",
        "timestamp": "{{eventDate}}"
      }
    }
  }
}

script:pre-request {
  bru.setVar('eventTime', new Date().toISOString());
  bru.setVar('eventDate', new Date().toISOString());
}

settings {
  encodeUrl: true
  timeout: 0
}
