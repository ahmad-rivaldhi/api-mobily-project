meta {
  name: FTTH RCY
  seq: 2
}

vars:pre-request {
  customerCategory: Royal
  networkCategory: FTTH RCY
}

docs {
  # FTTH RCY (Royal)

  Royal customer TMF 622 create-order variants (postpaid naming preserved in
  filenames). Mesh-extender buckets: `without ME` … `with 3 ME`.
}

auth {
  mode: inherit
}

