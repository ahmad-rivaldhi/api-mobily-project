meta {
  name: DOWIYAT
  seq: 4
}

docs {
  # DOWIYAT (folder spelling)

  Payload `infraProvider` values may remain **DAWIYAT** where the API expects the legacy enum â€” folders use **DOWIYAT** for consistency.
}

auth {
  mode: inherit
}

