meta {
  name: STC-TEST_FTTH_LINK
  type: http
  seq: 4
}

post {
  url: {{demo-mob-dev}}/serviceInstallationManagement/v1/notification/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "eventId": "Event-{{orderId}}",
    "eventTime": "{{eventDate}}",
    "eventType": "serviceOrderStateChangeNotification",
    "event": {
      "serviceOrder": {
        "funcId": "STC_CPE_UPDATE",
        "externalId": "{{stcInstallationId}}",
        "lineItemIdentifier": "",
        "lineItemStatus": "",
        "description": "",
        "orderStatus": "",
        "comments": "",
        "providerStatus": "ORDERED",
        "providerMilestone": "TEST_FTTH_LINK",
        "providerComments": "",
        "orderTransactionNo": "{{stcInstallationId}}",
        "serviceAccNum": "{{stcServiceAccNum}}",
        "devices": [
          {
            "integrationId": "{{cpeIntegrationId}}",
            "newSerialNumber": "{{cpeSerialNumber}}"
          }
        ]
      }
    }
  }
}

script:pre-request {
  bru.setVar('eventTime', new Date().toISOString());
  bru.setVar('eventDate', new Date().toISOString());
}

settings {
  encodeUrl: true
  timeout: 0
}
