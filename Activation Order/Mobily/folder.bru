meta {
  name: Mobily
  seq: 1
}

docs {
  # Mobily — activation auxiliaries

  Mobily-only requests that are not scoped to a single TMF 622 ME variant
  (for example appointment updates).
}

auth {
  mode: inherit
}
