meta {
  name: TMF-622 Create Sales Order
  seq: 2
}

docs {
  # TMF 622 — Create product order (Mobily infra)

  | Branch | Characteristics |
  |--------|-----------------|
  | **FTTH Consumer** | Regular — postpaid / prepaid, 0–3 ME |
  | **FTTH RCY** | Royal — postpaid variants, 0–3 ME |

  Folder `vars:pre-request` on **FTTH Consumer** and **FTTH RCY** sets
  `customerCategory` and `networkCategory` for all child requests.
}

auth {
  mode: inherit
}
