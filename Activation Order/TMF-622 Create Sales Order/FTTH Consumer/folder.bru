meta {
  name: FTTH Consumer
  seq: 1
}

vars:pre-request {
  customerCategory: Regular
  networkCategory: FTTH CONSUMER
}

docs {
  # FTTH Consumer (Regular)

  Postpaid and prepaid sub-folders; mesh-extender variants live one level below
  (`without ME`, `with 1 ME`, …).
}

auth {
  mode: inherit
}
